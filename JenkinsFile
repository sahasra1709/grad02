pipeline {
    agent any

    environment {
        JAVA_HOME = 'C:\\Program Files\\Java\\jdk-25'
        ORG_GRADLE_JAVA_HOME = 'C:\\Program Files\\Java\\jdk-25'
        PATH = "${JAVA_HOME}\\bin;${env.PATH}"
    }

    stages {
        stage('Build') {
            steps {
                bat 'gradlew --version'
                bat 'gradlew clean build'
            }
        }

        stage('Run') {
            steps {
                bat 'gradlew run'
            }
        }
    }
}
